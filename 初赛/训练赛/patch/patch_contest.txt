注意：如若您使用过旧的CPU硬件和系统版本导致无法运行，请下载以下对应的补丁包，以支持较旧的硬件和系统版本。此外，Windows版补丁包中包含一个keyboard_demo.exe，以解除键盘Demo对python的依赖。补丁包用法：解压在原目录上，覆盖对应文件即可。

WindowsRelease.patch0311.zip：
2023-03-11补丁内容：
去除了判题器对BMI2指令集的使用，以兼容一些比较旧的不支持BMI2指令集的CPU。如之前运行判题器后打印一条INFO日志之后就退出了，更新此补丁可以解决此问题。
去除了Windows版本键盘DEMO对Python的依赖，现在有一个keyboard_demo.exe可以直接运行。

LinuxRelease.patch0311.zip：
2023-03-11补丁内容：
去除了判题器对BMI2指令集的使用，以兼容一些比较旧的不支持BMI2指令集的CPU。如之前运行判题器后打印一条INFO日志之后就退出了，更新此补丁可以解决此问题。
降低了判题器对GLIBC的版本依赖，以兼容一些使用较低版本的Linux选手。目前对GLIBC的版本依赖为2.17（之前是2.29）

MacOSRelease.patch0311.zip：
2023-03-11补丁内容：
去除了判题器对BMI2指令集的使用，以兼容一些比较旧的不支持BMI2指令集的CPU。如之前运行判题器后打印一条INFO日志之后就退出了，更新此补丁可以解决此问题。